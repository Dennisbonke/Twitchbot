os: linux
dist: focal
language: cpp
compiler:
  - gcc
install:
  - sudo add-apt-repository --yes ppa:ubuntu-toolchain-r/test
  - sudo apt-get update -qq
  - sudo apt-get install -qq gcc-10 g++-10
script:
  - make