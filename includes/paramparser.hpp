#ifndef _paramparser_h
#define _paramparser_h

#include <vector>

class ParamParser {
    public:
        ParamParser();
        virtual ~ParamParser() = default;


    private:
        
};

#endif //_paramparser_h


/**
 *  ${sender} -> 
 *  ${user} ->
 *  ${count} -> 
 * ${sender}
 * ${sender}
 */